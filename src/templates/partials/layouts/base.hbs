<!DOCTYPE html>
<html>

<head>
  {{> includes/head }}
</head>

<body class="antialiased">
  {{#> precontent }}
  {{/precontent}}
  <div class="relative container max-w-screen-xl min-h-screen px-6 pt-2 md:pt-8 md:pt-6 md:px-12 mx-auto">
    {{> includes/navbar }}
    <hr class="h-12 sep" />
    {{#> content }}
    {{/content}}
  </div>
</body>

</html>